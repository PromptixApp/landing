'use client';

import Home from '../page';

export default function RuPage() {
  // Эта страница будет рендерить тот же контент, что и главная
  // Логика переключения языка обрабатывается в I18nProvider
  return <Home />;
} 