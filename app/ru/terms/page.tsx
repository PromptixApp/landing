'use client';

import TermsOfService from '../../terms/page';

export default function RuTermsPage() {
  // Эта страница будет рендерить тот же контент, что и основная страница условий
  // Логика переключения языка обрабатывается в I18nProvider
  return <TermsOfService />;
} 